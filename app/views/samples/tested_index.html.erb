<div class="wrapper">
  <div class="container">

    <ul>
      <% @samples.each do |sample| %>
        <li>
          <%= sample %>
          <%= link_to "PDF File", tested_index_samples_path(format:'pdf') %>
        </li>
      <% end %>
    </ul>
  </div>
</div>
