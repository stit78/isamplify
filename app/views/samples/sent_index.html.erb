<%= render 'shared/navbar2' %>
<div class="wrapper">
  <div class="container">

    <div class="header-pending-show">
      <div class="text-header-pending">
        <h2 class="text-center">Samples pending customer approval</h2>
      </div>
    </div>

    <% if @samples.count == 0 %>
      <p>No sent samples</p>
    <% else %>
      <% @samples.each do |sample| %>
        <%= render "shared/card_sample", sample: sample %>
      <% end %>
    <% end %>

  </div>
</div>
