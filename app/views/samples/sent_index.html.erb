<div class="wrapper">
  <h1>These following samples need to be emailed</h1>
  <div class="container">
    <ul class= "no-bullet">
      <% @samples.each do |sample| %>
          <li>
            <%= sample.id %>
            <%= render "shared/card_sample", sample: @sample %>
          </li>
      <% end %>
    </ul>
  </div>
</div>
